<Window x:Class="PL.Delivery.DeliveryInListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Delivery"
        mc:Ignorable="d"
        Title="Delivery List" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid HorizontalAlignment="Stretch" Height="Auto" Grid.Row="0" VerticalAlignment="Stretch" Width="Auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Sort By:" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center" Grid.Column="0"/>
            <ComboBox x:Name="CbSort" HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.Column="1" 
                      ItemsSource="{Binding SortOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                      SelectedItem="{Binding CurrentSort, RelativeSource={RelativeSource AncestorType=Window}}"
                      SelectionChanged="CbSort_SelectionChanged"
                      Margin="10,5,10,5"/>
            <Label Content="Filter:" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center" Grid.Column="2"/>
            <CheckBox x:Name="ChkActive" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="3" Margin="10,5,10,5"
                      IsThreeState="True"/>
        </Grid>
        <ListView Grid.Row="1" ItemsSource="{Binding DeliveryList, RelativeSource={RelativeSource AncestorType=Window}}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Full Name" Width="100" DisplayMemberBinding="{Binding FullName}"/>
                    <GridViewColumn Header="Is Active" Width="80" DisplayMemberBinding="{Binding IsActive}"/>
                    <GridViewColumn Header="Delivery Type" Width="100" DisplayMemberBinding="{Binding DeliveryType}"/>
                    <GridViewColumn Header="Employment Date" Width="120" DisplayMemberBinding="{Binding EmploymentStartDate}"/>
                    <GridViewColumn Header="In Time" Width="80" DisplayMemberBinding="{Binding SumOrderInTime}"/>
                    <GridViewColumn Header="Late" Width="80" DisplayMemberBinding="{Binding SumOrderInLate}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button x:Name="BtnAddDelivery" Content="Add Delivery" Grid.Row="2" HorizontalAlignment="Center" Height="40" Width="120" Click="BtnAddDelivery_Click" Margin="0,10,0,10"/>
    </Grid>
</Window>
