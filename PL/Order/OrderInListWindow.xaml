<Window x:Class="PL.Order.OrderInListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Order"
        mc:Ignorable="d"
        Title="Order List" Height="600" Width="1200">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Controls Row: Filter and Sort -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <!-- Order Status Filter Checkbox -->
            <Label Grid.Column="0" Content="Filter Status:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <CheckBox x:Name="ChkStatus" Grid.Column="1" IsThreeState="True" VerticalAlignment="Center"
                      IsChecked="{Binding FilterUI, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Sort ComboBox -->
            <Label Grid.Column="3" Content="Sort By:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>
            <ComboBox x:Name="CbSort" Grid.Column="4" 
                      ItemsSource="{Binding SortOptions}"
                      SelectedItem="{Binding CurrentSort, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectionChanged="CbSort_SelectionChanged"/>
        </Grid>

        <!-- ListView with Columns -->
        <ListView Grid.Row="1" ItemsSource="{Binding OrderList}" Margin="0,0,0,16">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Order ID" Width="80" DisplayMemberBinding="{Binding OrderId}"/>
                    <GridViewColumn Header="Delivery ID" Width="90" DisplayMemberBinding="{Binding DeliveryId}"/>
                    <GridViewColumn Header="Order Type" Width="100" DisplayMemberBinding="{Binding OrderType}"/>
                    <GridViewColumn Header="Air Distance" Width="120" DisplayMemberBinding="{Binding AirDistance}"/>
                    <GridViewColumn Header="Status" Width="100" DisplayMemberBinding="{Binding OrderStatus}"/>
                    <GridViewColumn Header="Schedule Status" Width="110" DisplayMemberBinding="{Binding ScheduleStatus}"/>
                    <GridViewColumn Header="Time Remaining" Width="110" DisplayMemberBinding="{Binding TimeRemaining}"/>
                    <GridViewColumn Header="Total Processing" Width="130" DisplayMemberBinding="{Binding TotalProcessingTime}"/>
                    <GridViewColumn Header="Deliveries" Width="80" DisplayMemberBinding="{Binding TotalDeliveries}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="0,0,16,0"/>
                    <Setter Property="Padding" Value="16,10"/>
                    <Setter Property="MinWidth" Value="100"/>
                </Style>
            </StackPanel.Resources>
            <Button x:Name="BtnAddOrder" Content="Add Order" Height="40" Click="BtnAddOrder_Click"/>
        </StackPanel>
    </Grid>
</Window>