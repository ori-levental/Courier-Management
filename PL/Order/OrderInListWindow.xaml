<Window x:Class="PL.Order.OrderListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Order"
        xmlns:bo="clr-namespace:BO;assembly=BL" 
        mc:Ignorable="d"
        Title="Order List Management" Height="640" Width="1050"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="39*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="2" Padding="10" Background="#F5F5F5" BorderBrush="Gray" BorderThickness="0,0,0,1">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">

                <Label Content="Sort:" FontWeight="Bold" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding SortOptions}"
                          SelectedItem="{Binding CurrentSort}" 
                          SelectionChanged="CbSort_SelectionChanged"
                          Width="110" Margin="0,0,15,0"/>

                <Label Content="Filter:" FontWeight="Bold" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding StatusOptions}"
                          SelectedItem="{Binding SelectedStatusFilter}"
                          SelectionChanged="CbFilter_SelectionChanged"
                          Width="110"/>
                <Button Content="X" Click="BtnClearFilter_Click" Width="25" Margin="5,0,15,0" FontWeight="Bold" ToolTip="Clear Filter"/>

                <CheckBox Content="Group by Status" 
                          IsChecked="{Binding IsGrouped, Mode=TwoWay}"
                          VerticalAlignment="Center" 
                          FontWeight="SemiBold"
                          Margin="10,0,0,0"/>
            </WrapPanel>
        </Border>

        <ListView Grid.Row="1" ItemsSource="{Binding Path=OrderList}" Margin="5">

            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border Background="LightGray" Padding="5" Margin="0,5,0,0" BorderBrush="Gray" BorderThickness="0,0,0,1">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Order ID" DisplayMemberBinding="{Binding OrderId}" Width="60"/>
                    <GridViewColumn Header="Del. ID" DisplayMemberBinding="{Binding DeliveryId, TargetNullValue='-'}" Width="60"/>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding OrderType}" Width="80"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding OrderStatus}" Width="90"/>
                    <GridViewColumn Header="Schedule" DisplayMemberBinding="{Binding ScheduleStatus}" Width="80"/>
                    <GridViewColumn Header="Dist (km)" DisplayMemberBinding="{Binding AirDistance, StringFormat=F2}" Width="70"/>

                    <GridViewColumn Header="Time Left" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TimeRemaining, StringFormat='hh\\:mm'}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Process Time" Width="90">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalProcessingTime, StringFormat='hh\\:mm\\:ss'}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Total Deliv." DisplayMemberBinding="{Binding TotalDeliveries}" Width="80"/>

                    <GridViewColumn Header="Actions" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Cancel" 
                                        Click="BtnCancel_Click"
                                        FontWeight="Bold" Foreground="White" Padding="5,2">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="IsEnabled" Value="False"/>
                                            <Setter Property="Background" Value="LightGray"/>
                                            <Setter Property="BorderBrush" Value="Gray"/>

                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding OrderStatus}" Value="Open">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                    <Setter Property="Background" Value="#FFD700"/>
                                                    <Setter Property="BorderBrush" Value="#DAA520"/>
                                                    <Setter Property="Cursor" Value="Hand"/>
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding OrderStatus}" Value="OnCare">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                    <Setter Property="Background" Value="#FFD700"/>
                                                    <Setter Property="BorderBrush" Value="#DAA520"/>
                                                    <Setter Property="Cursor" Value="Hand"/>
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="BorderBrush" Value="LightGray"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="Margin" Value="0,1,0,1"/>
                    <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Button Content="Add New Order" Grid.Row="2" HorizontalAlignment="Center" 
                Height="40" Width="150" Margin="0,10,0,10" FontWeight="Bold"
                Click="BtnAddOrder_Click"/>
    </Grid>
</Window>