<Window x:Class="PL.Order.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Order"
        xmlns:bo="clr-namespace:BO;assembly=BL"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Order Details" Height="650" Width="900"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Window.Resources>
        <ObjectDataProvider x:Key="OrderTypeEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bo:OrderType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5,5,5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Column="0" Header="Order Details" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="ID:" Grid.Row="0"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CurrentOrder.Id, Mode=OneWay}" IsEnabled="False" Background="#F0F0F0"/>

                    <Label Content="Customer Name:" Grid.Row="1"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CurrentOrder.OrderingName, Mode=TwoWay}"/>

                    <Label Content="Phone:" Grid.Row="2"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentOrder.PhoneNumber, Mode=TwoWay}"/>

                    <Label Content="Address:" Grid.Row="3"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CurrentOrder.FullAddress, Mode=TwoWay}" TextWrapping="Wrap" Height="50" AcceptsReturn="True"/>

                    <Label Content="Priority Type:" Grid.Row="4"/>
                    <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding Source={StaticResource OrderTypeEnum}}" SelectedItem="{Binding CurrentOrder.OrderType, Mode=TwoWay}" Height="25" Margin="0,5"/>

                    <Label Content="Notes:" Grid.Row="5"/>
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CurrentOrder.Description, Mode=TwoWay}" TextWrapping="Wrap" Height="60"/>
                </Grid>
            </ScrollViewer>
        </GroupBox>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="Status Information" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="Status:" Grid.Row="0"/>
                    <TextBlock Text="{Binding CurrentOrder.OrderStatus}" Grid.Row="0" Grid.Column="1" FontWeight="Bold" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentOrder.OrderStatus}" Value="Open">
                                        <Setter Property="Foreground" Value="Green"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CurrentOrder.OrderStatus}" Value="OnCare">
                                        <Setter Property="Foreground" Value="Blue"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CurrentOrder.OrderStatus}" Value="Cancelled">
                                        <Setter Property="Foreground" Value="Red"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CurrentOrder.OrderStatus}" Value="Provided">
                                        <Setter Property="Foreground" Value="Gray"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Label Content="Schedule:" Grid.Row="1"/>
                    <TextBlock Text="{Binding CurrentOrder.ScheduleStatus}" Grid.Row="1" Grid.Column="1" FontWeight="Bold" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentOrder.ScheduleStatus}" Value="OnTime">
                                        <Setter Property="Foreground" Value="Green"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CurrentOrder.ScheduleStatus}" Value="InRisk">
                                        <Setter Property="Foreground" Value="OrangeRed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CurrentOrder.ScheduleStatus}" Value="Late">
                                        <Setter Property="Foreground" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Label Content="Created At:" Grid.Row="2"/>
                    <TextBlock Text="{Binding CurrentOrder.StartOrderTime}" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center"/>

                    <Label Content="Deadline:" Grid.Row="3"/>
                    <TextBlock Text="{Binding CurrentOrder.MaxArrivalTime}" Grid.Row="3" Grid.Column="1" Foreground="Red" VerticalAlignment="Center"/>

                    <Label Content="Time Left:" Grid.Row="4"/>
                    <TextBlock Text="{Binding CurrentOrder.TimeRemaining, StringFormat={}{0:dd}d {0:hh}:{0:mm}:{0:ss}}" 
                               Grid.Row="4" Grid.Column="1" FontWeight="Bold" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentOrder.ScheduleStatus}" Value="Late">
                                        <Setter Property="Foreground" Value="Red"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CurrentOrder.ScheduleStatus}" Value="InRisk">
                                        <Setter Property="Foreground" Value="OrangeRed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="Delivery History">
                <ListView ItemsSource="{Binding CurrentOrder.DeliveryHistory}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" DisplayMemberBinding="{Binding DeliveryId}" Width="40"/>
                            <GridViewColumn Header="Courier" DisplayMemberBinding="{Binding CourierName}" Width="100"/>
                            <GridViewColumn Header="Status" DisplayMemberBinding="{Binding DeliveryEndType}" Width="80"/>
                            <GridViewColumn Header="Start" DisplayMemberBinding="{Binding DeliveryStartTime, StringFormat='dd/MM HH:mm'}" Width="90"/>
                            <GridViewColumn Header="End" DisplayMemberBinding="{Binding DeliveryEndTime, TargetNullValue='-', StringFormat='dd/MM HH:mm'}" Width="90"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>
        </Grid>

        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">

            <Button Content="{Binding ButtonContent}" 
                    Click="BtnAction_Click"
                    FontWeight="Bold" FontSize="16" Height="40" Width="200" Margin="10">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentOrder.OrderStatus}" Value="Provided">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding CurrentOrder.OrderStatus}" Value="Cancelled">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Cancel Order" 
                    Click="BtnCancel_Click"
                    Background="#FFD700"
                    FontWeight="Bold" FontSize="16" Height="40" Width="150" Margin="10">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Collapsed"/>

                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsAddMode}" Value="False"/>
                                    <Condition Binding="{Binding CurrentOrder.OrderStatus}" Value="Open"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>

                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsAddMode}" Value="False"/>
                                    <Condition Binding="{Binding CurrentOrder.OrderStatus}" Value="OnCare"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>

                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsAddMode}" Value="False"/>
                                    <Condition Binding="{Binding CurrentOrder.OrderStatus}" Value="NotFound"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>